# URL of the open-road.am Fastify API
API_BASE_URL=https://open-roads-am.onrender.com

# Transport: "stdio" (default, for Claude Desktop / Cursor) or "http"
TRANSPORT=stdio

# Port for HTTP transport (ignored in stdio mode)
PORT=3002
